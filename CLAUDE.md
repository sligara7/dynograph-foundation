# CLAUDE.md

**Read [AGENTS.md](AGENTS.md).** It is the primary instruction file for this repo and follows the [agents.md](https://agents.md) convention, so every agent working here reads the same thing.

This file exists only because some harnesses read `CLAUDE.md` first. Keeping the content in one place rather than duplicating it is deliberate: a rule only one collaborator's tool ever sees is worse than no rule at all.

> **reflow2 is installed here.** The design graph is this project's memory — read [AGENTS.md](AGENTS.md) and consult it before writing or changing code.
