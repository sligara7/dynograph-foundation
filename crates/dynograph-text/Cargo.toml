[package]
name = "dynograph-text"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
# Embedded inverted-index / BM25 search engine. Isolated to this crate so
# consumers that don't need full-text search never compile it — the storage
# and service layers pull `dynograph-text` in behind a feature flag.
tantivy = "0.26"
thiserror.workspace = true

[dev-dependencies]
tempfile = "3"
